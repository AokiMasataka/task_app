export type AuthInfo = {
    name: string,
    pass: string
};

export type ItemInfo = {
    name: string,
    id: string
};

export type UserInfo = {
    id: string,
    name: string,
    email: string
}

export type UserInfos = UserInfo[];